// Kept as a stable import surface: defaults/presets now live with their
// generator modules, and generateId in utils/id.
export { generateId } from './utils/id';
export { DEFAULT_PARAMS, PRESETS } from './generators/harmonograph';
export { DEFAULT_ATTRACTOR, ATTRACTOR_PRESETS } from './generators/attractor';
